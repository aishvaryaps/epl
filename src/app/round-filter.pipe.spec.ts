import { RoundFilterPipe } from './round-filter.pipe';

describe('RoundFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
