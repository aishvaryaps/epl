import { MatchByDatePipe } from './match-by-date.pipe';

describe('MatchByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MatchByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
